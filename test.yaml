- name: Test Playbook
  hosts: localhost
  tasks:
    - name: Git Clone
      ansible.builtin.git:
        repo: 'https://github.com/rhn-consulting-kemori/demo-itst-test.git'
        dest: /tmp/demo-itst-test

    - name: Include a play after another play
      ansible.builtin.import_playbook: test2.yaml
